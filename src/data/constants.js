export const SWIPE_DIRECTION = {
    LEFT: -1,
    RIGHT: 1,
}

export const DEFAULT_GAME_WORLD = Object.freeze({
    state: {
        environment: 40,
        people: 60,
        security: 75,
        money: 90,
    },
    flags: {},
})
